import { SvgContainerDirective } from './svg-container.directive';

describe('SvgContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgContainerDirective();
    expect(directive).toBeTruthy();
  });
});
